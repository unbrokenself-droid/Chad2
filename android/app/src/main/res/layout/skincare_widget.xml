<?xml version="1.0" encoding="utf-8"?>
<!--
  Layout for SkincareWidgetProvider — mirrors hydration_widget.xml's
  structure exactly (see that file's comment for why); only the ids,
  default text, and button label differ.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/skincare_widget_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Skincare"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/skincare_widget_summary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0 of 0 steps"
            android:textColor="#B0B0B0"
            android:textSize="12sp"/>
    </LinearLayout>

    <ProgressBar
        android:id="@+id/skincare_widget_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="8dp"
        android:layout_marginTop="10dp"
        android:max="100"
        android:progress="0"
        android:progressTint="#2962FF"
        android:progressBackgroundTint="#2A2A2A"/>

    <TextView
        android:id="@+id/skincare_widget_check_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:background="@drawable/widget_button_background"
        android:text="Check Next Step"
        android:textColor="#FFFFFF"
        android:textSize="13sp"
        android:textStyle="bold"
        android:gravity="center"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"/>

</LinearLayout>
